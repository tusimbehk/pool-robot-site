"use client";

import { Analytics } from "@/components/analytics";

/**
 * Analytics wrapper component for client-side only rendering
 */
export function AnalyticsWrapper() {
  return <Analytics />;
}
